# Visoraft 完整交付门槛

一项能力只有同时满足以下条件，才可以在功能矩阵中标记为完成：

1. React 19 页面可发现、可操作，包含空态、加载、成功、失败和恢复路径。
2. Go API 具备输入校验、持久化、幂等、权限边界和稳定错误模型。
3. Go/Python Worker 执行真实业务逻辑，不以固定返回值或定时等待冒充处理。
4. 前端、API、队列、Worker、数据库和对象存储完成本地联调。
5. 自动化测试覆盖主路径、失败路径、重试/恢复和重启后的状态保持。
6. 涉及 AcFun、Bilibili、阿里云等外部平台时，需同时保留本地契约测试与真实凭证环境验证记录。
7. 功能清单与原项目逐项对照，无未解释缺口。

在全部条目满足前，项目整体状态只能是 `in_progress`。
